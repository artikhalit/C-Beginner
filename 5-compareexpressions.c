#include<stdio.h>
#include<stdlib.h>

int main()
{
    int score;
    score=61;
    (score>=60)? printf("Y\n"):printf("X...\n");
// "?" means 'if' ":" means else 
// this situation exists  only one code lines. 
    return 0;
}